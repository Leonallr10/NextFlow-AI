/**
 * Default Gemini model for new Google AI Studio keys.
 * `gemini-2.0-flash` is not available to new projects — use a current ID from:
 * https://ai.google.dev/gemini-api/docs/models
 */
export const DEFAULT_GEMINI_MODEL = "gemini-2.5-flash";
